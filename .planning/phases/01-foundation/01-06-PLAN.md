---
phase: 01-foundation
plan: 06
type: execute
wave: 4
depends_on: ["01-02", "01-03", "01-04", "01-05"]
files_modified: []
autonomous: false
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - PROJ-01
  - PROJ-02
  - PROJ-03
  - PROJ-04
  - PROJ-05
  - LLM-01
  - LLM-02
  - LLM-03
  - LLM-04

must_haves:
  truths:
    - "All Phase 1 success criteria are met end-to-end"
    - "Auth flow works: sign up, verify email, sign in, stay logged in, sign out, reset password"
    - "Dashboard shows project cards or empty state"
    - "Project CRUD works: create, view list, navigate to project, delete"
    - "Settings page manages API key securely and model preferences persist"
    - "n8n pipeline test endpoint works (if n8n configured)"
  artifacts: []
  key_links: []
---

<objective>
Run the complete Phase 1 verification checklist end-to-end. Confirm that all user-facing features work together as an integrated system.

Purpose: Catch integration issues before Phase 2 builds on this foundation. Ensure every Phase 1 success criterion from the roadmap is met.
Output: Verified Phase 1 — all auth, dashboard, settings, and n8n features working together.
</objective>

<execution_context>
@/Users/nickthacker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickthacker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
@.planning/phases/01-foundation/01-04-SUMMARY.md
@.planning/phases/01-foundation/01-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated verification checks</name>
  <files></files>
  <action>
Run the following automated checks and collect results:

1. **Build check:** `npm run build` — must succeed with zero errors
2. **Type check:** `npx tsc --noEmit` — must pass with zero errors
3. **File existence check:** Verify all critical files exist:
   - `middleware.ts`
   - `src/lib/supabase/server.ts`
   - `src/lib/supabase/client.ts`
   - `src/lib/supabase/middleware.ts`
   - `src/lib/n8n/client.ts`
   - `src/actions/auth.ts`
   - `src/actions/projects.ts`
   - `src/actions/settings.ts`
   - `src/app/(auth)/login/page.tsx`
   - `src/app/(dashboard)/dashboard/page.tsx`
   - `src/app/(dashboard)/settings/page.tsx`
   - `src/app/api/health/route.ts`
   - `supabase/migrations/00001_initial_schema.sql`
4. **Security check:** Grep the entire codebase for `NEXT_PUBLIC_N8N` or `NEXT_PUBLIC_OPENROUTER` or `NEXT_PUBLIC_.*SECRET` — must return zero results
5. **RLS check:** Verify migration file has `enable row level security` for all 3 tables
6. **Health endpoint:** `curl -s http://localhost:3000/api/health` — should return JSON with `status: 'ok'`

Report results as a checklist. If any check fails, fix the issue before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - All automated checks pass
    - No security issues found
    - Build succeeds, types compile, all files exist
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of all Phase 1 features end-to-end</name>
  <files></files>
  <action>
What was built: Complete Phase 1 — Authentication, Project Dashboard, API Key Management, n8n Pipeline.

Start the dev server with `npm run dev` and verify the following:

**1. Auth Flow (AUTH-01 through AUTH-04):**
- Visit http://localhost:3000 — should redirect to /login
- Click "Create account" tab
- Enter an email and password (min 8 chars) — click Create Account
- Check your email for verification link (if using real Supabase, the email will arrive; if using Supabase local dev, check the Inbucket UI)
- Click the verification link — should redirect to /dashboard
- Sign out (button in nav bar) — should redirect to /login
- Sign back in with same credentials — should land on /dashboard
- Close browser, reopen, visit http://localhost:3000 — should still be logged in (session persistence)
- Click "Forgot password?" — enter email — check for reset email

**2. Dashboard (PROJ-01 through PROJ-04):**
- As a new user, confirm you see the empty state with example cards and "Start your novel" CTA
- Click "Start Your Novel" (or "New Project" button)
- Enter a title and select a genre — create the project
- Confirm the project card appears with: title, status badge (draft), genre tag, word count (0), and progress bar
- Create a second project — confirm both appear in the grid
- Click the three-dot menu on a project — delete it — confirm it disappears
- Click a project card — confirm it navigates to /projects/[id] (page may be empty, that's expected)

**3. Settings (LLM-01 through LLM-04):**
- Navigate to /settings
- Confirm you see the API key section and model preferences section
- Enter an OpenRouter API key — click "Test Connection" — confirm it reports valid/invalid
- Save the key — confirm it shows as masked (••••••••abcd)
- Refresh the page — confirm the masked key persists
- Open browser DevTools Network tab — confirm no raw API key appears in any response
- Change a model preference (e.g., change prose model) — confirm it auto-saves
- Refresh — confirm model preferences persisted

**4. Health Check:**
- Visit http://localhost:3000/api/health in browser — confirm JSON response with status "ok"

Resume signal: Type "approved" if all checks pass, or describe any issues found.
  </action>
  <verify>User confirms all features work by typing "approved"</verify>
  <done>
    - All auth flows verified by human tester
    - Dashboard CRUD verified by human tester
    - Settings API key and model preferences verified by human tester
    - No security issues observed in browser DevTools
  </done>
</task>

</tasks>

<verification>
All Phase 1 roadmap success criteria verified:
1. User can create an account, log in, stay logged in across browser sessions, log out from any page, and reset their password via email
2. User can view a dashboard listing all their novel projects with title, status, word count, and last modified date
3. User can create, resume, and delete novel projects
4. User can connect their own OpenRouter API key (BYOK) and select LLMs per task; key is never visible in the browser
5. Users without an API key can access generation via a hosted subscription option (schema ready, billing deferred to Phase 5)
</verification>

<success_criteria>
- All automated checks pass (build, types, security, file existence)
- Human verification confirms all user-facing flows work end-to-end
- No security issues: API keys masked, no secrets in browser, RLS enforced
- Phase 1 is complete and ready for Phase 2 to build on
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-06-SUMMARY.md`
</output>
