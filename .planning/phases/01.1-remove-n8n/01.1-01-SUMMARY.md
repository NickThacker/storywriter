---
phase: 01.1-remove-n8n
plan: 01
subsystem: infra
tags: [n8n, cleanup, openrouter, nextjs, typescript]

# Dependency graph
requires:
  - phase: 01-foundation
    provides: n8n webhook client (src/lib/n8n/client.ts), test endpoint, and env vars that are being removed
provides:
  - Clean codebase with zero n8n references in src/ directory
  - Simplified health endpoint returning {status, timestamp} only
  - Supabase-only .env.local.example template
  - Updated 02-04-PLAN.md using direct OpenRouter calls instead of triggerN8nWorkflow
affects:
  - 02-guided-intake-and-outline (premise-prefill route now targets OpenRouter directly)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Direct OpenRouter fetch pattern replaces n8n webhook orchestration for AI calls"

key-files:
  created:
    - .env.local.example (Supabase-only template, no n8n vars)
  modified:
    - src/app/api/health/route.ts (simplified to {status, timestamp}, no n8n import)
    - .planning/STATE.md (removed n8n todos, added n8n removal decision)
    - .planning/phases/02-guided-intake-and-outline/02-04-PLAN.md (OpenRouter instead of triggerN8nWorkflow)
  deleted:
    - src/lib/n8n/client.ts
    - src/app/api/n8n/test/route.ts

key-decisions:
  - "n8n removed entirely — all AI orchestration goes through Next.js Route Handlers + Server Actions calling OpenRouter directly"
  - "02-04-PLAN.md Task 2 updated to use direct OpenRouter fetch with Vault-retrieved API key, falling back to mock when no key configured"

patterns-established:
  - "Stack is Next.js + Supabase + OpenRouter only — no webhook intermediary"

requirements-completed:
  - N/A-CLEANUP

# Metrics
duration: 2min
completed: 2026-03-01
---

# Phase 01.1 Plan 01: Remove n8n Summary

**Stripped n8n webhook client, test endpoint, and all env vars — codebase is now Next.js + Supabase + OpenRouter only with zero n8n references in src/**

## Performance

- **Duration:** ~2 min
- **Started:** 2026-03-01T18:49:11Z
- **Completed:** 2026-03-01T18:51:41Z
- **Tasks:** 2
- **Files modified:** 5 (plus 2 deleted, 1 created)

## Accomplishments
- Deleted src/lib/n8n/client.ts (N8nError, isN8nConfigured, triggerN8nWorkflow) and src/app/api/n8n/test/route.ts
- Simplified src/app/api/health/route.ts to return only {status, timestamp} — no n8n import
- Cleaned .env.local (N8N_BASE_URL, N8N_WEBHOOK_SECRET removed) and recreated .env.local.example as Supabase-only template
- Updated 02-04-PLAN.md Task 2 to use direct OpenRouter fetch instead of triggerN8nWorkflow
- Removed stale .next build cache that held a type reference to the deleted n8n test route

## Task Commits

Each task was committed atomically:

1. **Task 1: Delete n8n source files, simplify health route, clean env files** - `6a40cbc` (chore)
2. **Task 2: Update planning docs to remove forward-looking n8n references** - `98490af` (chore)

**Plan metadata:** (final docs commit — see below)

## Files Created/Modified
- `src/app/api/health/route.ts` - Simplified: returns {status, timestamp} only, no n8n import
- `src/lib/n8n/client.ts` - DELETED
- `src/app/api/n8n/test/route.ts` - DELETED
- `.env.local` - Removed N8N_BASE_URL and N8N_WEBHOOK_SECRET lines (gitignored, local change)
- `.env.local.example` - Recreated with Supabase-only vars (no n8n entries)
- `.planning/STATE.md` - Removed n8n pending todos, added n8n removal decision
- `.planning/phases/02-guided-intake-and-outline/02-04-PLAN.md` - Updated Task 2 from triggerN8nWorkflow to direct OpenRouter fetch

## Decisions Made
- n8n removed entirely: 30s webhook timeout blocks all heavy AI tasks (outline 60-120s, chapters 60-300s), streaming cannot relay through webhooks, and all generation already routes directly through Next.js. Simplifies stack from 3 services to 2.
- 02-04-PLAN.md updated to direct OpenRouter pattern: check user's openrouter_api_key_ref in user_settings; retrieve from Vault if present; return mock stub if absent (enabling dev without API key).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Removed stale .next build cache with reference to deleted n8n route**
- **Found during:** Task 1 verification (npx tsc --noEmit)
- **Issue:** .next/types/validator.ts held a type reference to src/app/api/n8n/test/route.js which no longer existed, causing a TS2307 error
- **Fix:** Ran `rm -rf .next` to clear the stale build cache; subsequent tsc --noEmit passed cleanly
- **Files modified:** .next/ (removed)
- **Verification:** TypeScript compiled with zero errors after cache removal
- **Committed in:** 6a40cbc (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking — stale build cache)
**Impact on plan:** Essential fix; stale cache would have falsely failed the TypeScript verification step. No scope creep.

## Issues Encountered
- .env.local is gitignored (correct — contains secrets). The n8n var removal is a local-only change; .env.local.example is the committed artifact showing the new template.

## User Setup Required
None - no external service configuration required. The removal of n8n vars from .env.local is a local operation (file is gitignored).

## Next Phase Readiness
- Stack is clean: Next.js + Supabase + OpenRouter only
- Phase 2 (02-guided-intake-and-outline) can begin; 02-04-PLAN.md Task 2 is already updated for direct OpenRouter calls
- TypeScript compiles cleanly with zero errors

---
*Phase: 01.1-remove-n8n*
*Completed: 2026-03-01*
