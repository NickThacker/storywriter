---
phase: 01.1-remove-n8n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/n8n/client.ts
  - src/app/api/n8n/test/route.ts
  - src/app/api/health/route.ts
  - .env.local
  - .env.local.example
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/02-guided-intake-and-outline/02-04-PLAN.md
autonomous: true
requirements:
  - N/A-CLEANUP

must_haves:
  truths:
    - "No source file imports from src/lib/n8n/"
    - "Health endpoint returns {status, timestamp} without n8n_configured field"
    - "TypeScript compiles with zero errors after n8n removal"
    - ".env.local contains no N8N_ environment variables"
    - "02-04-PLAN.md references direct OpenRouter calls instead of triggerN8nWorkflow"
    - "ROADMAP.md Phase 4 description contains no n8n references"
  artifacts:
    - path: "src/app/api/health/route.ts"
      provides: "Simplified health endpoint without n8n dependency"
      contains: "status.*ok"
    - path: ".env.local.example"
      provides: "Developer env template without n8n vars"
      contains: "NEXT_PUBLIC_SUPABASE_URL"
  key_links:
    - from: "src/app/api/health/route.ts"
      to: "next/server"
      via: "NextResponse.json only — no n8n import"
      pattern: "import.*NextResponse"
---

<objective>
Remove all n8n code, configuration, and forward-looking references from the StoryWriter codebase. After this plan executes, the project's architecture is Next.js + Supabase + OpenRouter only — no n8n webhook client, test endpoint, or environment variables remain.

Purpose: n8n adds complexity the project systematically works around. Its 30s webhook timeout blocks all heavy AI tasks, streaming cannot relay through webhooks, and all generation already routes directly through Next.js. Removing it simplifies the stack from 3 services to 2.
Output: Clean codebase with no n8n references in source code or env files, plus updated planning docs so Phase 2 execution does not reference deleted modules.
</objective>

<execution_context>
@/Users/nickthacker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickthacker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1-remove-n8n/01.1-RESEARCH.md

<interfaces>
From src/app/api/health/route.ts (BEFORE — will be simplified):
```typescript
import { NextResponse } from 'next/server'
import { isN8nConfigured } from '@/lib/n8n/client'

export async function GET() {
  return NextResponse.json(
    {
      status: 'ok',
      timestamp: new Date().toISOString(),
      n8n_configured: isN8nConfigured(),
    },
    { status: 200 }
  )
}
```

From src/lib/n8n/client.ts (BEING DELETED):
```typescript
export class N8nError extends Error { ... }
export function isN8nConfigured(): boolean
export async function triggerN8nWorkflow(webhookPath: string, payload: Record<string, unknown>): Promise<unknown>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete n8n source files, simplify health route, clean env files</name>
  <files>
    src/lib/n8n/client.ts
    src/app/api/n8n/test/route.ts
    src/app/api/health/route.ts
    .env.local
    .env.local.example
  </files>
  <action>
**Step 1 — Verify no unexpected n8n importers exist:**
```bash
grep -r "from.*n8n" src/
grep -r "n8n/client" src/
```
Expected: only 3 files (client.ts, test/route.ts, health/route.ts). If any other file appears, fix it before proceeding.

**Step 2 — Simplify `src/app/api/health/route.ts`:**
Replace the entire file with:
```typescript
import { NextResponse } from 'next/server'

export async function GET() {
  return NextResponse.json(
    {
      status: 'ok',
      timestamp: new Date().toISOString(),
    },
    { status: 200 }
  )
}
```
Remove the `isN8nConfigured` import and the `n8n_configured` field from the response. This MUST happen before or in the same step as deleting client.ts to avoid TypeScript compile failure.

**Step 3 — Delete n8n files and directory:**
```bash
rm src/lib/n8n/client.ts
rm src/app/api/n8n/test/route.ts
rmdir src/app/api/n8n/test
rmdir src/app/api/n8n
```
Also remove the empty `src/lib/n8n/` directory:
```bash
rmdir src/lib/n8n
```

**Step 4 — Clean `.env.local`:**
Remove lines 14-16 (the n8n comment and two vars). The file should end after `NEXT_PUBLIC_SITE_URL=http://localhost:3000` with no trailing n8n block. Final content:
```
# Supabase — Project Settings -> API -> Project URL
NEXT_PUBLIC_SUPABASE_URL=<existing value>

# Supabase — Project Settings -> API -> anon/public key (safe for browser)
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=<existing value>

# Supabase service role — NEVER expose to browser
# Project Settings -> API -> service_role key
SUPABASE_SERVICE_ROLE_KEY=<existing value>

# App URL — for auth redirects
NEXT_PUBLIC_SITE_URL=http://localhost:3000
```
Keep all existing values intact. Only remove the n8n lines.

**Step 5 — Recreate `.env.local.example`:**
The file is currently staged as deleted in git. Create a new `.env.local.example` without n8n vars:
```
# Supabase — Project Settings -> API -> Project URL
NEXT_PUBLIC_SUPABASE_URL=

# Supabase — Project Settings -> API -> anon/public key (safe for browser)
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Supabase service role — NEVER expose to browser
# Project Settings -> API -> service_role key
SUPABASE_SERVICE_ROLE_KEY=

# App URL — for auth redirects
NEXT_PUBLIC_SITE_URL=http://localhost:3000
```

**Step 6 — Verify TypeScript compiles:**
```bash
npx tsc --noEmit
```
Must pass with zero errors. If it fails, the health route still has the n8n import — fix it.
  </action>
  <verify>
    <automated>cd /Users/nickthacker/storywriter && npx tsc --noEmit 2>&1 | tail -5 && grep -r "n8n" src/ | wc -l | xargs -I{} test {} -eq 0 && echo "PASS: no n8n refs in src/" || echo "FAIL: n8n refs still exist"</automated>
  </verify>
  <done>
    - src/lib/n8n/ directory does not exist
    - src/app/api/n8n/ directory does not exist
    - src/app/api/health/route.ts returns {status, timestamp} only — no n8n import
    - .env.local has no N8N_BASE_URL or N8N_WEBHOOK_SECRET lines
    - .env.local.example exists with Supabase-only template
    - `npx tsc --noEmit` passes
    - `grep -r "n8n" src/` returns zero results
  </done>
</task>

<task type="auto">
  <name>Task 2: Update planning docs to remove forward-looking n8n references</name>
  <files>
    .planning/ROADMAP.md
    .planning/STATE.md
    .planning/phases/02-guided-intake-and-outline/02-04-PLAN.md
  </files>
  <action>
**Step 1 — Update `.planning/ROADMAP.md` Phase 4 description:**
Find the Phase 4 entry. Current text includes "n8n Wait node HITL". Replace the Phase 4 description line:

Before: `- [ ] **Phase 4: Creative Checkpoints** - Between-chapter approval loop, plot direction choices, n8n Wait node HITL`
After: `- [ ] **Phase 4: Creative Checkpoints** - Between-chapter approval loop, plot direction choices, human-in-the-loop checkpoints`

Do NOT change any other Phase 4 content (Goal, Requirements, Success Criteria, etc. are fine as-is).

**Step 2 — Update `.planning/STATE.md` Pending Todos:**
Remove these two n8n-related entries from the `### Pending Todos` section:
- "Set N8N_BASE_URL and N8N_WEBHOOK_SECRET in .env.local to test n8n pipeline"
- "Create n8n test workflow (POST /webhook/test with IF node secret validation) to exercise /api/n8n/test"

Keep the other pending todos (Supabase setup, migrations) — those are still valid.

Also in `### Decisions`, add a new entry:
- `[Phase 01.1]: n8n removed from codebase — all AI orchestration goes through Next.js Route Handlers + Server Actions calling OpenRouter directly`

Also in `### Roadmap Evolution`, the existing entry about Phase 01.1 should remain as-is (it's accurate history).

**Step 3 — Update `.planning/phases/02-guided-intake-and-outline/02-04-PLAN.md`:**
This plan's Task 2 (premise prefill API route) references `triggerN8nWorkflow` and `isN8nConfigured`. Update it to use direct OpenRouter calls instead.

Changes needed in 02-04-PLAN.md:

a) In **frontmatter `must_haves`**: Update the truth "Premise prefill API route calls n8n (or returns mock when n8n not configured)" to: "Premise prefill API route calls OpenRouter directly (or returns mock when no API key configured)"

b) In **frontmatter `key_links`**: Replace the key_link from `premise-prefill/route.ts` to `src/lib/n8n/client.ts` with a link to OpenRouter:
   - from: "src/app/api/generate/premise-prefill/route.ts"
   - to: "OpenRouter API"
   - via: "Direct fetch to openrouter.ai/api/v1/chat/completions"
   - pattern: "openrouter\\.ai|fetch.*chat/completions"

c) In **`<interfaces>` section**: Remove the `From src/lib/n8n/client.ts:` block entirely.

d) In **Task 2 action** (premise prefill route): Replace the n8n-based implementation with a direct OpenRouter call:
   - Remove step 3's `isN8nConfigured()` check and both branches
   - Replace with: Check if user has an API key configured (`openrouter_api_key_ref` in user_settings). If no API key: return mock prefill response (keep the existing mock JSON). If API key exists: retrieve decrypted key from Vault using the established pattern from Phase 01-05 settings.ts, then call OpenRouter directly with `fetch('https://openrouter.ai/api/v1/chat/completions', ...)` with a system prompt asking it to infer genre/themes/setting/tone/characters from the premise. Use `response_format: { type: 'json_object' }` for structured output.
   - Remove step 4's reference to "passing to n8n"
   - Update error handling: remove 502 for n8n failure, add 502 for OpenRouter failure

e) In **Task 2 done criteria**: Replace "Returns mock prefill when n8n not configured" with "Returns mock prefill when no API key configured". Replace "Calls triggerN8nWorkflow when n8n is configured" with "Calls OpenRouter directly when API key configured".

Do NOT change Tasks 1, 3, or 4 in 02-04-PLAN.md — they are unaffected.
  </action>
  <verify>
    <automated>cd /Users/nickthacker/storywriter && grep -c "n8n" .planning/ROADMAP.md | xargs -I{} bash -c 'echo "ROADMAP n8n refs: {}"' && grep -c "triggerN8nWorkflow\|isN8nConfigured\|n8n/client" .planning/phases/02-guided-intake-and-outline/02-04-PLAN.md | xargs -I{} bash -c 'echo "02-04 n8n code refs: {}"'</automated>
  </verify>
  <done>
    - ROADMAP.md Phase 4 description says "human-in-the-loop checkpoints" not "n8n Wait node HITL"
    - STATE.md Pending Todos has no n8n-related entries
    - STATE.md Decisions includes the n8n removal decision
    - 02-04-PLAN.md Task 2 describes direct OpenRouter calls, not triggerN8nWorkflow
    - 02-04-PLAN.md must_haves and key_links reference OpenRouter, not n8n/client.ts
    - 02-04-PLAN.md interfaces section has no n8n/client.ts block
    - Tasks 1, 3, 4 in 02-04-PLAN.md are unchanged
  </done>
</task>

</tasks>

<verification>
1. `grep -r "n8n" src/` returns zero results — no n8n references in any source file
2. `npx tsc --noEmit` passes — TypeScript compiles cleanly after deletion
3. `curl http://localhost:3000/api/health` returns `{"status":"ok","timestamp":"..."}` with no `n8n_configured` field (if dev server running)
4. `.env.local` and `.env.local.example` contain no N8N_ variables
5. `grep "triggerN8nWorkflow\|isN8nConfigured\|n8n/client" .planning/phases/02-guided-intake-and-outline/02-04-PLAN.md` returns zero results
6. `grep "n8n Wait node" .planning/ROADMAP.md` returns zero results
7. `grep "N8N_" .planning/STATE.md` returns zero results (in Pending Todos section)
</verification>

<success_criteria>
- Zero n8n references in source code (src/ directory)
- TypeScript compiles cleanly
- Health endpoint simplified to {status, timestamp}
- Environment files cleaned of n8n variables
- Downstream Phase 2 plan (02-04) updated to use direct OpenRouter pattern
- ROADMAP and STATE planning docs scrubbed of forward-looking n8n references
- Historical planning docs (01-03-PLAN.md, 01-03-SUMMARY.md) left untouched
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-remove-n8n/01.1-01-SUMMARY.md`
</output>
