---
phase: 02-guided-intake-and-outline
plan: 09
type: execute
wave: 6
depends_on:
  - 02-03
  - 02-04
  - 02-05
  - 02-06
  - 02-07
  - 02-08
files_modified:
  - .planning/phases/02-guided-intake-and-outline/02-09-VERIFICATION.md
autonomous: false
requirements:
  - INTK-01
  - INTK-02
  - INTK-03
  - INTK-04
  - OUTL-01
  - OUTL-02
  - OUTL-03
  - OUTL-04
  - OUTL-05
  - CHAR-01
  - CHAR-02
  - CHAR-03
  - CHAR-04

must_haves:
  truths:
    - "User can complete the full intake wizard from path selection through review"
    - "User can generate an outline from intake data and watch it stream"
    - "User can view, edit, and approve the outline"
    - "Approved outline populates the story bible with characters and locations"
    - "User can create and edit character profiles on the story bible page"
    - "Story bible schema supports context injection for Phase 3"
  artifacts:
    - path: ".planning/phases/02-guided-intake-and-outline/02-09-VERIFICATION.md"
      provides: "Verification report documenting all checks"
      contains: "PASS|FAIL"
  key_links:
    - from: "src/app/(dashboard)/projects/[id]/intake/page.tsx"
      to: "src/app/api/generate/outline/route.ts"
      via: "Wizard Generate button triggers outline SSE stream"
      pattern: "api/generate/outline"
    - from: "src/actions/outline.ts"
      to: "src/actions/story-bible.ts"
      via: "approveOutline calls seedStoryBibleFromOutline"
      pattern: "seedStoryBibleFromOutline"
---

<objective>
End-to-end verification of all Phase 2 functionality. Run automated checks, verify the complete user journey from intake to approved story bible, and document results.

Purpose: Ensure all 13 requirements are met and the system works as an integrated whole before moving to Phase 3 (chapter generation).
Output: Verification report documenting pass/fail status for each requirement.
</objective>

<execution_context>
@/Users/nickthacker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickthacker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-guided-intake-and-outline/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated verification checks</name>
  <files>
    .planning/phases/02-guided-intake-and-outline/02-09-VERIFICATION.md
  </files>
  <action>
Run the following automated checks and document results:

**Build verification:**
```bash
npm run build 2>&1 | tail -20
npx tsc --noEmit 2>&1 | tail -20
```

**File existence checks — verify all key artifacts exist:**
- `supabase/migrations/00002_story_bible_tables.sql`
- `src/lib/stores/intake-store.ts`
- `src/components/intake/intake-store-provider.tsx`
- `src/components/intake/card-picker.tsx`
- `src/lib/data/genres.ts`, `themes.ts`, `tones.ts`, `settings.ts`, `beat-sheets.ts`, `lengths.ts`
- `src/app/(dashboard)/projects/[id]/intake/page.tsx`
- `src/app/(dashboard)/projects/[id]/outline/page.tsx`
- `src/app/(dashboard)/projects/[id]/story-bible/page.tsx`
- `src/app/api/generate/outline/route.ts`
- `src/app/api/generate/premise-prefill/route.ts`
- `src/actions/intake.ts`
- `src/actions/outline.ts`
- `src/actions/story-bible.ts`
- `src/hooks/use-outline-stream.ts`

**Key link verification — grep for critical connections:**
- Intake page imports useIntakeStore
- Outline route has `force-dynamic` directive
- Outline route calls OpenRouter (not n8n) for generation
- approveOutline calls seedStoryBibleFromOutline
- Story bible page queries characters, locations, world_facts tables
- Characters table has `source` column in migration

**Schema verification:**
- Migration has RLS on all 4 tables
- Migration has update_updated_at triggers
- Database types include all new tables

Document all results in a verification report at `.planning/phases/02-guided-intake-and-outline/02-09-VERIFICATION.md`. For each check, record PASS or FAIL with details.

Fix any FAIL items discovered during verification. If a fix requires code changes, make them and re-verify.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - All automated checks documented with PASS/FAIL
    - Build succeeds without errors
    - TypeScript compiles without errors
    - All key artifacts exist
    - Critical connections verified via grep
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual end-to-end verification</name>
  <files>
    .planning/phases/02-guided-intake-and-outline/02-09-VERIFICATION.md
  </files>
  <action>
    Verify the complete Phase 2 user journey manually:
    - Multi-step intake wizard with card-based selections (5 creative steps + review)
    - Premise paste path with AI pre-fill
    - AI outline generation with SSE streaming via OpenRouter
    - Two-panel outline editor with chapter list, detail view, and beat sheet overlay
    - Outline regeneration (full, directed, per-chapter) and approval flow
    - Story bible page with Characters, Locations, Timeline, and World Facts tabs
    - Character profile creation and editing with structured fields
    - Story bible seeding from approved outline
  </action>
  <verify>
    1. Start dev server: `npm run dev`
    2. Log in and create a new project from the dashboard
    3. Verify redirect to intake wizard at `/projects/[id]/intake`
    4. Walk through the wizard: select genre, themes, characters, setting, tone/structure
    5. Verify review screen shows all selections with edit buttons
    6. (If API key configured) Click "Generate Outline" and watch streaming generation
    7. (If no API key) Verify appropriate error message
    8. On outline page: verify two-panel layout with chapter list and detail
    9. Click chapters in list — detail panel should update
    10. Try inline editing a chapter title and summary
    11. Open beat sheet overlay — verify beat-to-chapter mappings
    12. Try switching beat sheet in dropdown — visualization should update
    13. Click "Approve Outline" — verify story bible is populated
    14. Navigate to story bible page — verify Characters and Locations tabs have data
    15. Try creating a new character manually — verify it saves
    16. Try editing a character profile — verify auto-save
    17. Verify project status shows "writing" on dashboard after approval
  </verify>
  <done>
    All 17 verification steps pass. End-to-end flow works from intake wizard through approved story bible.
  </done>
</task>

</tasks>

<verification>
All Phase 2 requirements verified:
- INTK-01: Multi-step interview guides user through genre, themes, characters, setting, tone
- INTK-02: Premise paste path with AI pre-fill
- INTK-03: Review screen shows all answers before generation
- INTK-04: Decision-driven card UI (not text boxes)
- OUTL-01: AI generates full outline from intake data
- OUTL-02: User reviews outline in two-panel layout
- OUTL-03: User edits outline with inline editing
- OUTL-04: Target length and chapter count set during intake
- OUTL-05: Approved outline populates story bible
- CHAR-01: Character profiles with structured fields
- CHAR-02: Characters editable at any time
- CHAR-03: Story bible tracks characters, locations, timeline, world facts
- CHAR-04: Schema designed for context injection (normalized tables with project_id FK)
</verification>

<success_criteria>
- All 13 requirements pass automated and manual verification
- Build succeeds, TypeScript compiles
- End-to-end user journey works from intake wizard through approved story bible
- Phase 2 is complete and ready for Phase 3 (chapter generation)
</success_criteria>

<output>
After completion, create `.planning/phases/02-guided-intake-and-outline/02-09-SUMMARY.md`
</output>
