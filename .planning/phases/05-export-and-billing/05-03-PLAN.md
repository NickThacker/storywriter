---
phase: 05-export-and-billing
plan: 03
type: execute
wave: 2
depends_on: [05-02]
files_modified:
  - src/components/export/export-dialog.tsx
  - src/app/(dashboard)/projects/[id]/chapters/page.tsx
autonomous: true
requirements: [EXPT-01, EXPT-02, EXPT-03]

must_haves:
  truths:
    - "User can open an export dialog from the project chapters page"
    - "User can choose export format (DOCX, ePub, RTF, TXT)"
    - "User can choose to include approved-only or all chapters"
    - "User can enter a pen name for the export"
    - "Clicking export triggers a file download"
  artifacts:
    - path: "src/components/export/export-dialog.tsx"
      provides: "Export dialog with format picker, chapter filter, pen name input"
      exports: ["ExportDialog"]
    - path: "src/app/(dashboard)/projects/[id]/chapters/page.tsx"
      provides: "Chapters page with export button triggering export dialog"
      contains: "ExportDialog"
  key_links:
    - from: "src/components/export/export-dialog.tsx"
      to: "/api/export/[projectId]"
      via: "window.location.href or anchor download"
      pattern: "/api/export/"
---

<objective>
Build the export dialog UI component and integrate it into the project chapters page.

Purpose: Users need an accessible way to trigger novel export. Per CONTEXT.md, the export action lives in the project settings/actions area. Since no dedicated project settings page exists, the chapters page header serves as the project-level actions area — this is the page where users manage their novel content and project-level actions naturally live. The export button will appear in the chapters page header area (not in the chapter list toolbar or adjacent to any individual chapter).
Output: ExportDialog component and integration into chapters page.
</objective>

<execution_context>
@/Users/nickthacker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickthacker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-export-and-billing/05-02-SUMMARY.md

<interfaces>
<!-- Export route API (created in Plan 02) -->
GET /api/export/{projectId}?format=docx|epub|rtf|txt&include=approved|all&penName=Author+Name
Returns: File download with Content-Disposition header

<!-- Radix UI components already in the project -->
From package.json:
- @radix-ui/react-dialog (for modal dialog)
- @radix-ui/react-select (for dropdowns)
- @radix-ui/react-label

<!-- Existing chapters page structure -->
From src/app/(dashboard)/projects/[id]/chapters/page.tsx:
- Server component that renders chapter management UI
- Has access to project ID from params
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ExportDialog component</name>
  <files>src/components/export/export-dialog.tsx</files>
  <action>
Create `src/components/export/export-dialog.tsx` as a client component (`'use client'`):

**Props:**
```typescript
interface ExportDialogProps {
  projectId: string
  projectTitle: string
  userName: string    // default for pen name
}
```

**UI structure** (use Radix Dialog + existing shadcn/ui patterns):
1. Trigger button: `<Button variant="outline">` with a download/export icon from lucide-react (`Download` or `FileDown`)
2. Dialog content:
   - Title: "Export Novel"
   - Description: "Download your novel as a formatted document."
   - **Format selector**: Radio group or Radix Select with four options:
     - DOCX (.docx) -- "Microsoft Word format"
     - ePub (.epub) -- "E-reader format"
     - RTF (.rtf) -- "Rich text, compatible with Vellum"
     - Plain Text (.txt) -- "Simple text file"
   - **Chapter filter**: Radio group with two options:
     - "Approved chapters only" (default)
     - "All chapters (drafts marked as [DRAFT])"
   - **Pen name**: Text input, pre-filled with `userName`, editable
   - **Export button**: Primary button "Download {format}" that triggers the download
   - **Cancel button**: Secondary button to close dialog

**Download behavior:**
- Construct URL: `/api/export/${projectId}?format=${format}&include=${include}&penName=${encodeURIComponent(penName)}`
- Open in new tab or use `window.location.href` to trigger browser download
- Show loading state on the export button while download initiates
- After triggering, close the dialog

**Styling:** Follow existing component patterns -- use `cn()` utility, Tailwind classes, `rounded-lg`, `border`, consistent with settings/api-key-form.tsx and other existing dialog components. Use existing Radix Dialog primitives (`DialogRoot`, `DialogTrigger`, `DialogContent`, etc.) or the shadcn `Dialog` wrapper if present.

Do NOT use `@radix-ui/react-radio-group` (not in package.json). Instead, use a simple button-group pattern with Tailwind styling for format and include selection (styled buttons that toggle active state). Or use `@radix-ui/react-select` which IS installed.
  </action>
  <verify>
    <automated>cd /Users/nickthacker/storywriter && npx tsc --noEmit 2>&1 | grep -i "export-dialog" | head -5</automated>
  </verify>
  <done>ExportDialog component renders a dialog with format picker, chapter filter, pen name input, and download button. Clicking download constructs the correct API URL and triggers a file download.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate ExportDialog into chapters page header</name>
  <files>src/app/(dashboard)/projects/[id]/chapters/page.tsx</files>
  <action>
Read the existing chapters page to understand its current structure. Then add the ExportDialog:

1. Import `ExportDialog` from `@/components/export/export-dialog`
2. The chapters page already fetches the project (for chapter data). Extract `project.title` and fetch the user's name (from Supabase auth `user.user_metadata.full_name` or `user.email` as fallback).
3. Add the `<ExportDialog>` component in the **page-level header area** (the top-level heading row with the project title and any existing project-level action buttons). Pass `projectId`, `projectTitle`, and `userName` props.
4. The ExportDialog is a client component, so wrap it appropriately if the page is a server component. Pass the needed props as serializable values.

PLACEMENT RATIONALE (per CONTEXT.md decision "Export action lives in the project settings/actions area, not in the chapters page toolbar"): Since no dedicated project settings page exists, the chapters page header is the project-level actions area. Place the export button in the top-level page header alongside the project title -- NOT inside the chapter list, NOT next to individual chapter items, NOT in any per-chapter action menu. The header is project-scoped; the chapter list is chapter-scoped.

Do NOT modify the chapter list or chapter content areas. Only add the export button/dialog to the page header.
  </action>
  <verify>
    <automated>cd /Users/nickthacker/storywriter && grep -l "ExportDialog" src/app/\(dashboard\)/projects/\[id\]/chapters/page.tsx</automated>
  </verify>
  <done>Chapters page includes ExportDialog in the page header (project-level area, not chapter toolbar), passing project ID, title, and user name. Users can click the export button to open the dialog and download their novel.</done>
</task>

</tasks>

<verification>
- ExportDialog component exists and renders without errors
- Chapters page includes the ExportDialog component in the page header (not chapter toolbar)
- `npm run build` compiles both components without errors
- Export dialog opens, shows format options, chapter filter, and pen name input
</verification>

<success_criteria>
- User can open the export dialog from the chapters page header
- User can select any of the four export formats
- User can choose approved-only or all-chapters inclusion
- User can edit their pen name before exporting
- Clicking download triggers the correct API route with proper query params
</success_criteria>

<output>
After completion, create `.planning/phases/05-export-and-billing/05-03-SUMMARY.md`
</output>
