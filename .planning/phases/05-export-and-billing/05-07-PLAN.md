---
phase: 05-export-and-billing
plan: 07
type: execute
wave: 5
depends_on: [05-03, 05-06]
files_modified: []
autonomous: false
requirements: [EXPT-01, EXPT-02, EXPT-03, BILL-01, BILL-02, BILL-03, BILL-04]

must_haves:
  truths:
    - "User can export their novel as DOCX, ePub, RTF, or plain text"
    - "Export assembles approved chapters into a single document with title page and chapter structure"
    - "Hosted tier users can subscribe to a plan via Stripe Checkout"
    - "Token usage is tracked per generation request"
    - "User is warned at 80% budget usage"
    - "Generation is blocked at 100% budget"
    - "BYOK users bypass all billing"
  artifacts: []
  key_links: []
---

<objective>
End-to-end verification of all Phase 5 requirements: export formats, billing flow, token tracking, and BYOK bypass.

Purpose: Confirm all EXPT and BILL requirements are met before marking Phase 5 complete.
Output: Verification results — pass or fail for each requirement.
</objective>

<execution_context>
@/Users/nickthacker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickthacker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-export-and-billing/05-02-SUMMARY.md
@.planning/phases/05-export-and-billing/05-03-SUMMARY.md
@.planning/phases/05-export-and-billing/05-04-SUMMARY.md
@.planning/phases/05-export-and-billing/05-05-SUMMARY.md
@.planning/phases/05-export-and-billing/05-06-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:automated" gate="blocking">
  <name>Task 1: Automated verification of export and billing</name>
  <action>
Run automated checks and report results as a checklist with pass/fail for each:

1. **Build check**: `npm run build` must succeed with no errors
2. **Type check**: `npm run type-check` must succeed
3. **Export route exists**: Verify `/api/export/[projectId]/route.ts` exports `GET`
4. **Export format modules exist**: Verify all four builders exist and export their functions
5. **Webhook route exists**: Verify `/api/webhooks/stripe/route.ts` exports `POST`
6. **Billing actions exist**: Verify `createCheckoutSession`, `createPortalSession`, `getBillingStatus` exports
7. **Token tracking exists**: Verify `checkTokenBudget`, `createTokenInterceptStream` exports
8. **Budget check in routes**: Verify all four generation route handlers import `checkTokenBudget`
9. **Budget warning wired**: Verify `use-chapter-stream.ts` imports and calls `checkBudgetWarning`
10. **Usage page exists**: Verify `/usage/page.tsx` exists
11. **BYOK bypass**: Verify `isByok` check exists in budget-check.ts and billing-section.tsx
12. **Migration exists**: Verify `00005_billing_and_token_tracking.sql` exists with required tables
13. **Stripe client exists**: Verify `src/lib/stripe/client.ts` exists
  </action>
  <verify>
    <automated>cd /Users/nickthacker/storywriter && npm run build 2>&1 | tail -5</automated>
  </verify>
  <resume-signal>Paste checklist results. Type "continue" to proceed to human verification, or describe build failures for gap closure.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 5: Export and Billing

**Export (EXPT-01, EXPT-02, EXPT-03):**
- Export dialog accessible from chapters page
- Four formats: DOCX, ePub, RTF, plain text
- Chapter filtering (approved only vs all with draft markers)
- Pen name on title page
- Full book structure: title page, table of contents, chapter headers, page breaks

**Billing (BILL-01, BILL-02, BILL-03, BILL-04):**
- Stripe subscription tiers: Starter ($9/mo, 500K tokens), Writer ($19/mo, 2M tokens), Pro ($39/mo, 5M tokens)
- Credit pack purchases on top of subscription
- Token tracking per generation request with per-chapter granularity
- Budget enforcement: warning at 80%, hard block at 100%
- /usage page with budget bar and per-project usage
- Billing section in settings for subscription management
- BYOK users see no billing UI at all
  </what-built>
  <how-to-verify>
**NOTE: Stripe features require Stripe dashboard setup (products, prices, webhooks). If Stripe is not configured, verify export features only and skip billing verification.**

**Migration:**
1. Apply `supabase/migrations/00005_billing_and_token_tracking.sql` in Supabase SQL Editor

**Export verification:**
1. Navigate to a project with approved chapters at `/projects/{id}/chapters`
2. Click the Export button in the page header
3. Verify the export dialog opens with format options (DOCX, ePub, RTF, TXT)
4. Verify chapter filter options (approved only / all chapters) are present
5. Verify pen name field is pre-filled with your name
6. Export as DOCX -- verify the downloaded file opens in Word/Google Docs with title page, chapter headers, and page breaks
7. Export as TXT -- verify plain text file has chapter structure
8. (Optional) Export as ePub and verify in an e-reader app
9. (Optional) Export as RTF and verify opens in Vellum or Word

**Billing verification (if Stripe configured):**
1. Visit `/settings` -- verify billing section appears (if no API key set)
2. Subscribe to Starter tier -- verify Stripe Checkout opens
3. After subscription, verify `/usage` page shows budget bar
4. Generate a chapter -- verify token usage appears on /usage page
5. With an API key set (BYOK), verify NO billing UI appears anywhere
6. Visit `/usage` directly as BYOK user -- should redirect to dashboard

**Budget warning verification:**
1. (Simulated) If near 80% budget, generate a chapter and check for toast notification
2. (Simulated) If at 100% budget, try to generate -- should see upgrade modal
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 5 complete, or describe issues for gap closure</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- `npm run type-check` succeeds
- All PLAN.md summaries for plans 01-06 exist
- Export dialog is functional and triggers file downloads
- Billing pipeline is wired end-to-end (if Stripe configured)
- BYOK users see no billing UI
</verification>

<success_criteria>
All Phase 5 requirements verified:
- EXPT-01: User can export to DOCX
- EXPT-02: User can export to plain text
- EXPT-03: Export assembles approved chapters into single document
- BILL-01: Hosted tier users can subscribe with token/credit budget
- BILL-02: Token usage tracked per user and per project
- BILL-03: User warned at budget limit (toast fires via use-chapter-stream.ts -> checkBudgetWarning)
- BILL-04: BYOK users bypass billing
</success_criteria>

<output>
After completion, create `.planning/phases/05-export-and-billing/05-07-SUMMARY.md`
</output>
