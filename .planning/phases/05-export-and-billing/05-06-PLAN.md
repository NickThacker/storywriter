---
phase: 05-export-and-billing
plan: 06
type: execute
wave: 4
depends_on: [05-04, 05-05]
files_modified:
  - src/app/(dashboard)/usage/page.tsx
  - src/components/billing/usage-bar.tsx
  - src/components/billing/plan-card.tsx
  - src/components/billing/upgrade-modal.tsx
  - src/components/billing/billing-section.tsx
  - src/app/(dashboard)/settings/page.tsx
  - src/app/(dashboard)/layout.tsx
  - src/hooks/use-budget-warning.ts
  - src/hooks/use-chapter-stream.ts
autonomous: true
requirements: [BILL-01, BILL-02, BILL-03, BILL-04]

must_haves:
  truths:
    - "User can visit /usage and see their token usage for the current billing period"
    - "User sees a budget progress bar showing used vs remaining tokens"
    - "User sees per-project token totals on the usage page"
    - "User is warned via toast notification at 80% budget usage after generation completes"
    - "User sees a hard-block modal at 100% usage with upgrade/credit-pack options"
    - "Settings page includes a billing section with subscription management for hosted tier"
    - "BYOK users see NO billing UI anywhere -- no /usage link, no upgrade prompts, no budget bars"
  artifacts:
    - path: "src/app/(dashboard)/usage/page.tsx"
      provides: "Token usage dashboard page"
      exports: ["default"]
    - path: "src/components/billing/usage-bar.tsx"
      provides: "Budget progress bar component"
      exports: ["UsageBar"]
    - path: "src/components/billing/plan-card.tsx"
      provides: "Subscription tier card component"
      exports: ["PlanCard"]
    - path: "src/components/billing/upgrade-modal.tsx"
      provides: "Hard-block upgrade modal (shown at 100% usage)"
      exports: ["UpgradeModal"]
    - path: "src/components/billing/billing-section.tsx"
      provides: "Billing management section for settings page"
      exports: ["BillingSection"]
    - path: "src/hooks/use-budget-warning.ts"
      provides: "Hook that checks X-Budget-Warning header and shows toast"
      exports: ["useBudgetWarning", "checkBudgetWarning"]
    - path: "src/hooks/use-chapter-stream.ts"
      provides: "Chapter streaming hook wired to call checkBudgetWarning after SSE connection"
      contains: "checkBudgetWarning"
  key_links:
    - from: "src/app/(dashboard)/usage/page.tsx"
      to: "src/actions/token-usage.ts"
      via: "getUserTotalUsage, getProjectTokenUsage calls"
      pattern: "getUserTotalUsage"
    - from: "src/components/billing/upgrade-modal.tsx"
      to: "src/actions/billing.ts"
      via: "createCheckoutSession, createCreditPackSession actions"
      pattern: "createCheckoutSession"
    - from: "src/hooks/use-budget-warning.ts"
      to: "sonner toast"
      via: "Shows toast when X-Budget-Warning header is present"
      pattern: "toast"
    - from: "src/hooks/use-chapter-stream.ts"
      to: "src/hooks/use-budget-warning.ts"
      via: "calls checkBudgetWarning(response) after SSE response received"
      pattern: "checkBudgetWarning"
    - from: "src/app/(dashboard)/layout.tsx"
      to: "BYOK check"
      via: "Conditionally renders /usage nav link based on isByok"
      pattern: "isByok"
---

<objective>
Build the billing UI: usage page, budget warnings, upgrade modal, and billing section in settings. Wire the budget warning hook into the chapter streaming hook so the 80% toast actually fires. BYOK users see none of this.

Purpose: Hosted tier users need to see their usage, receive budget warnings, and manage their subscription. This is the user-facing billing experience.
Output: Usage page, billing components, budget warning system, settings integration, chapter stream wiring.
</objective>

<execution_context>
@/Users/nickthacker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickthacker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-export-and-billing/05-RESEARCH.md
@.planning/phases/05-export-and-billing/05-04-SUMMARY.md
@.planning/phases/05-export-and-billing/05-05-SUMMARY.md

<interfaces>
<!-- Billing actions from Plan 04 -->
From src/actions/billing.ts:
```typescript
export async function getBillingStatus(): Promise<BillingStatus>
export async function createCheckoutSession(priceId: string): Promise<{ url: string | null } | { error: string }>
export async function createCreditPackSession(priceId: string): Promise<{ url: string | null } | { error: string }>
export async function createPortalSession(): Promise<{ url: string | null } | { error: string }>
```

<!-- Token usage actions from Plan 05 -->
From src/actions/token-usage.ts:
```typescript
export async function getProjectTokenUsage(): Promise<{ data: { projectId: string, projectTitle: string, totalTokens: number }[] } | { error: string }>
export async function getUserTotalUsage(): Promise<{ data: { totalTokens: number, periodStart: string | null, periodEnd: string | null } } | { error: string }>
```

<!-- Tier config from Plan 04 -->
From src/lib/stripe/tiers.ts:
```typescript
export const TIERS: TierConfig[]
export const CREDIT_PACKS: CreditPackConfig[]
```

<!-- Billing types from Plan 01 -->
From src/types/billing.ts:
```typescript
export interface BillingStatus {
  isByok: boolean
  tier: SubscriptionTier
  tokenBudgetTotal: number
  tokenBudgetRemaining: number
  creditPackTokens: number
  billingPeriodEnd: string | null
  usagePercent: number
}
```

<!-- Existing toast (sonner) already in project -->
From package.json: "sonner": "^2.0.7"

<!-- Dashboard layout (has nav links) -->
From src/app/(dashboard)/layout.tsx:
// Contains nav with links to /dashboard, /settings, etc.

<!-- Chapter stream hook (to be modified in Task 2) -->
From src/hooks/use-chapter-stream.ts:
// Client hook that initiates SSE connection to /api/generate/chapter
// After fetch(), the Response object is available before piping the body stream
// checkBudgetWarning must be called with this Response object
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create billing UI components</name>
  <files>src/components/billing/usage-bar.tsx, src/components/billing/plan-card.tsx, src/components/billing/upgrade-modal.tsx, src/components/billing/billing-section.tsx</files>
  <action>
All components are client components (`'use client'`).

**`src/components/billing/usage-bar.tsx`:**
- Props: `{ used: number, total: number, creditPack: number, className?: string }`
- Render a horizontal progress bar showing token usage:
  - Bar background: gray, fill: blue (green when < 50%, yellow at 50-80%, red at 80%+)
  - Label above: "Used: {formatTokens(used)} / {formatTokens(total)}" (format: 500K, 1.2M, etc.)
  - If `creditPack > 0`, show secondary label: "+ {formatTokens(creditPack)} credit pack tokens"
  - Percentage label on right: "{percent}%"
  - If over 80%, show warning text: "Approaching budget limit"
  - If at 100%, show "Budget exhausted" in red
- Use Tailwind for styling. The bar should be `h-3 rounded-full`.
- `formatTokens(n: number): string` helper: format large numbers as K/M.

**`src/components/billing/plan-card.tsx`:**
- Props: `{ tier: TierConfig, isCurrentTier: boolean, onSelect: (priceId: string) => void }`
- Render a card showing:
  - Tier name (Starter / Writer / Pro)
  - Price: $X/mo
  - Monthly tokens: formatted
  - Feature bullet points (derive from tokens: "~N novels per month" based on ~500K tokens per novel)
  - If current tier: "Current Plan" badge, no select button
  - If not current: "Subscribe" button calling onSelect
- Use Tailwind card styling: `rounded-lg border p-6 shadow-sm`

**`src/components/billing/upgrade-modal.tsx`:**
- Props: `{ open: boolean, onClose: () => void, billingStatus: BillingStatus }`
- Radix Dialog modal that appears when generation is blocked at 100% usage
- Content:
  - Title: "Token Budget Exhausted"
  - Description: "You've used all your tokens for this billing period."
  - Three plan cards (using PlanCard component) for upgrading
  - Credit pack section: buttons for each credit pack option
  - "Manage Subscription" link (calls createPortalSession)
  - Close button
- On plan/pack select: call `createCheckoutSession` or `createCreditPackSession`, redirect to returned URL
- Show loading states during Stripe redirect

**`src/components/billing/billing-section.tsx`:**
- Props: `{ billingStatus: BillingStatus }`
- For the settings page. Shows:
  - Current plan name and status
  - UsageBar with current budget
  - "Manage Subscription" button (calls createPortalSession)
  - "Buy Credit Pack" section with pack options
  - Next billing date
- If `billingStatus.tier === 'none'`: show subscription tier cards for initial signup
- If tier is active: show current plan details + manage/upgrade options

Styling: Follow existing settings page patterns (sections with h2 headings, cards with borders, consistent spacing).
  </action>
  <verify>
    <automated>cd /Users/nickthacker/storywriter && npx tsc --noEmit 2>&1 | grep -i "billing/" | head -10</automated>
  </verify>
  <done>Four billing components exist: UsageBar (progress bar), PlanCard (tier display), UpgradeModal (hard-block at 100%), BillingSection (settings integration). All are client components with correct props.</done>
</task>

<task type="auto">
  <name>Task 2: Create usage page, budget warning hook, wire into chapter stream, and integrate billing into settings/layout</name>
  <files>src/app/(dashboard)/usage/page.tsx, src/hooks/use-budget-warning.ts, src/hooks/use-chapter-stream.ts, src/app/(dashboard)/settings/page.tsx, src/app/(dashboard)/layout.tsx</files>
  <action>
**Create `src/app/(dashboard)/usage/page.tsx`:**
- Server component
- Authenticate user (redirect to /login if not)
- Call `getBillingStatus()` -- if `isByok`, redirect to `/dashboard` (BYOK users should never see this page)
- Call `getUserTotalUsage()` and `getProjectTokenUsage()` in parallel
- Render:
  - Page title: "Token Usage"
  - UsageBar component with billing status data
  - Summary card: "Current Period: {periodStart} - {periodEnd}", total tokens used
  - Credit pack balance (if > 0)
  - Per-project breakdown: list of projects with project title and total tokens, sorted by tokens descending
  - If over 80% budget: persistent warning banner "You're approaching your token budget limit. Consider upgrading or purchasing a credit pack."
  - Link to Settings for subscription management
- Metadata: `title: 'Usage — StoryWriter'`

**Create `src/hooks/use-budget-warning.ts`:**
- Client-side hook: `useBudgetWarning()`
- Export a standalone function `checkBudgetWarning(response: Response): void` that:
  - Checks the `X-Budget-Warning` response header
  - If header value is `'near_limit'`, shows a Sonner toast: "You've used 80% of your monthly token budget. Consider upgrading or buying a credit pack." with an action link to /usage
  - Import `toast` from `sonner`
- The hook itself (`useBudgetWarning`) can be a thin wrapper that returns `checkBudgetWarning` for component use (or simply re-export the function). The important thing is that `checkBudgetWarning` is exported and callable from outside a React component (it has no hook dependencies).

**Modify `src/hooks/use-chapter-stream.ts`:**
- Read the existing file first to understand its current structure.
- Import `checkBudgetWarning` from `@/hooks/use-budget-warning`
- After the `fetch()` call to `/api/generate/chapter` succeeds and the Response is available (before piping the body stream), call `checkBudgetWarning(response)`.
- This is the critical wiring: the X-Budget-Warning header is set by the route handler in Plan 05, and this is where it gets checked to trigger the 80% toast (BILL-03).
- Do NOT modify any other logic in use-chapter-stream.ts -- only add the import and the single `checkBudgetWarning(response)` call.

**Modify `src/app/(dashboard)/settings/page.tsx`:**
- Add import for `getBillingStatus` from `@/actions/billing`
- Add import for `BillingSection` from `@/components/billing/billing-section`
- Call `getBillingStatus()` alongside existing data fetches
- If `billingStatus.isByok` is false, render `<BillingSection billingStatus={billingStatus} />` as a new section after the Model Preferences section
- If `billingStatus.isByok` is true, do NOT render any billing section (BYOK invisible per CONTEXT.md)
- Add a heading "Billing & Subscription" for the section

**Modify `src/app/(dashboard)/layout.tsx`:**
- Add a nav link to `/usage` page
- This link should ONLY appear for non-BYOK users
- Fetch billing status (or a lighter `isByok` check) to determine visibility
- If isByok, hide the /usage nav link entirely
- Use the existing nav structure and styling
- To avoid fetching billing status on every page load, consider using a simpler check: query `user_settings` for `openrouter_api_key` presence. If key exists -> BYOK -> hide /usage link.
  </action>
  <verify>
    <automated>cd /Users/nickthacker/storywriter && grep -l "checkBudgetWarning" src/hooks/use-chapter-stream.ts && npx tsc --noEmit 2>&1 | grep -i "use-budget-warning\|use-chapter-stream" | head -10</automated>
  </verify>
  <done>Usage page renders token usage data with budget bar and per-project breakdown. Budget warning hook exports checkBudgetWarning function. use-chapter-stream.ts calls checkBudgetWarning(response) after SSE fetch, wiring the 80% toast (BILL-03). Settings page includes billing section for non-BYOK users. Dashboard layout conditionally shows /usage nav link. BYOK users see no billing UI.</done>
</task>

</tasks>

<verification>
- `/usage` page renders for hosted tier users with budget bar, period info, and per-project usage
- `/usage` redirects BYOK users to dashboard
- Settings page shows billing section for non-BYOK users
- Settings page hides billing section for BYOK users
- Dashboard nav shows /usage link only for non-BYOK users
- `checkBudgetWarning` is imported and called in `use-chapter-stream.ts` after the fetch response
- Budget warning hook fires toast from sonner when X-Budget-Warning header is present
- UpgradeModal renders with tier cards and credit pack options
- `npm run type-check` passes
- `npm run build` succeeds
</verification>

<success_criteria>
- Hosted tier users can see their token usage and budget status on /usage
- Budget warnings appear as toast at 80% (wired via use-chapter-stream.ts calling checkBudgetWarning) and modal at 100%
- Subscription management is accessible from settings
- Credit pack purchases are available from the upgrade modal
- BYOK users see absolutely zero billing UI
</success_criteria>

<output>
After completion, create `.planning/phases/05-export-and-billing/05-06-SUMMARY.md`
</output>
