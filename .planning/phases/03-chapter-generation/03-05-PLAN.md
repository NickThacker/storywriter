---
phase: 03-chapter-generation
plan: 05
type: execute
wave: 4
depends_on: [03-04]
files_modified:
  - src/app/(dashboard)/projects/[id]/page.tsx
  - src/components/dashboard/project-card.tsx
autonomous: false
requirements: [PROG-02, PROG-03]

must_haves:
  truths:
    - "User with an approved outline is routed to /projects/[id]/chapters when visiting the project page"
    - "Project dashboard card shows chapter progress (X/Y chapters) and word count for projects in 'writing' status"
    - "User always knows which phase they are in when navigating between project pages"
  artifacts:
    - path: "src/app/(dashboard)/projects/[id]/page.tsx"
      provides: "Project router with writing status → chapters redirect"
      exports: ["default (page component)"]
    - path: "src/components/dashboard/project-card.tsx"
      provides: "Dashboard card with chapter progress display"
  key_links:
    - from: "src/app/(dashboard)/projects/[id]/page.tsx"
      to: "/projects/[id]/chapters"
      via: "redirect when status is writing"
      pattern: "redirect.*chapters"
---

<objective>
Update the project page router to redirect 'writing' status projects to the chapters page, and add chapter progress display to the project dashboard card. End with a human verification checkpoint.

Purpose: This completes the navigation loop — users with approved outlines land on the chapters page, and the dashboard shows their writing progress.

Output: Updated router and dashboard card, plus end-to-end verification.
</objective>

<execution_context>
@/Users/nickthacker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickthacker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/app/(dashboard)/projects/[id]/page.tsx

@.planning/phases/03-chapter-generation/03-04-SUMMARY.md

<interfaces>
<!-- Existing project page router -->
From src/app/(dashboard)/projects/[id]/page.tsx:
```typescript
// Current routing logic:
// status === 'draft' → outline or intake
// status === 'writing' → outline (placeholder — needs update to chapters)
// fallback → outline
```

<!-- Project card may or may not exist — check first -->
<!-- Dashboard card may be inline in the dashboard page -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update project page router for chapters redirect</name>
  <files>src/app/(dashboard)/projects/[id]/page.tsx</files>
  <action>
Update the project page router to redirect 'writing' status projects to the chapters page.

Current code has this comment: `// Phase 3 will add chapter view routing`

Change the `status === 'writing'` block from:
```typescript
if (status === 'writing') {
  redirect(`/projects/${id}/outline`)
}
```

To:
```typescript
if (status === 'writing') {
  redirect(`/projects/${id}/chapters`)
}
```

This is a one-line change. The chapters page (Plan 03-03) handles the case where the user might want to navigate to the outline from the chapters page (via PhaseNav).

Also update the 'complete' fallback to redirect to chapters instead of outline:
```typescript
// Fallback for any other status (e.g. 'complete')
redirect(`/projects/${id}/chapters`)
```
  </action>
  <verify>
    <automated>grep -n "chapters" src/app/\(dashboard\)/projects/\[id\]/page.tsx</automated>
  </verify>
  <done>Projects with 'writing' status redirect to /chapters instead of /outline</done>
</task>

<task type="auto">
  <name>Task 2: Add chapter progress display to project dashboard</name>
  <files>src/components/dashboard/project-card.tsx</files>
  <action>
First, check if `src/components/dashboard/project-card.tsx` exists. If it doesn't, find where dashboard project cards are rendered (likely inline in the dashboard page or in `create-project-dialog.tsx`).

If the project card exists as a component, add a progress indicator that shows:
- For projects with status 'writing': "{chapters_done}/{chapter_count} chapters | {word_count} words"
- A small progress bar (thin bar below the card content) with fill = `chapters_done / chapter_count * 100`%
- Use `text-xs text-muted-foreground` for the text

If the project card is inline in the dashboard page, extract the relevant section and add the progress info there.

The `ProjectRow` type already has `word_count`, `chapter_count`, and `chapters_done` fields. These are updated by the `updateProjectWordCount` action from Plan 03-01.

If no dedicated project-card component exists and the dashboard renders projects inline, add the progress display directly in the dashboard page's project rendering section. Use a conditional: only show the progress bar when `project.status === 'writing'` and `project.chapter_count > 0`.

Styling: Match the existing card design. Add the progress section below the existing card content (title, genre, word count). Use a thin `h-1 bg-muted rounded-full` track with a `bg-primary` fill.
  </action>
  <verify>
    <automated>grep -rn "chapters_done\|chapter_count" src/components/dashboard/ src/app/\(dashboard\)/ 2>/dev/null | head -10</automated>
  </verify>
  <done>Dashboard project cards show chapter progress (X/Y chapters, word count, progress bar) for projects in writing status</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 3 chapter generation system:
1. Chapter streaming hook with pause/stop/resume
2. Chapter server actions (save prose, fetch checkpoints, update word count)
3. Chapter generation route with rewrite adjustments support
4. Tiptap rich text editor with scene breaks, author notes, auto-save
5. Chapters page with server-side data loading
6. Chapter list with status badges (pending/generating/checkpoint/approved)
7. Streaming prose view with typing cursor effect
8. Chapter panel orchestrator tying everything together
9. Rewrite dialog for style/tone adjustments
10. Phase navigation showing all novel-writing phases
11. Progress bar with milestone markers
12. Project router update (writing status → chapters)
13. Dashboard progress display
  </what-built>
  <how-to-verify>
**Pre-requisite:** Ensure `00003_project_memory.sql` has been applied to Supabase. If not, run it in the SQL Editor first.

1. Visit the dashboard. Open a project that has an approved outline (status should be 'writing').
2. Verify you are redirected to `/projects/[id]/chapters` (not /outline).
3. Verify the chapter list shows all chapters from the outline with "Pending" status badges.
4. Verify the phase nav at the top shows "Chapters" as the current active phase.
5. Verify the progress bar shows "0/N chapters" and "0 words".
6. Click "Generate Next Chapter" or the Generate button on Chapter 1.
7. Verify prose streams in the right panel with a typing cursor effect.
8. Verify live word count updates during streaming.
9. Try clicking "Pause" during streaming — verify text stays visible and streaming stops.
10. After a chapter completes, verify the chapter list updates to show "Draft" status with word count.
11. Click on the completed chapter — verify the Tiptap editor opens with the chapter prose.
12. Make an edit in the editor — verify auto-save fires (no explicit save button).
13. Click "Rewrite" on the chapter — verify the adjustments dialog opens.
14. Enter adjustments and click "Rewrite Chapter" — verify a new stream starts.
15. Return to the dashboard — verify the project card shows chapter progress.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- Project router redirects 'writing' status to /chapters
- Dashboard shows chapter progress for writing-status projects
- Full generation → compression → save lifecycle works end to end
- Editor loads saved prose and auto-saves edits
- Rewrite flow works with adjustments
</verification>

<success_criteria>
- User with approved outline lands on chapters page via project router
- Dashboard shows chapter progress (X/Y chapters, word count, progress bar)
- Complete end-to-end flow verified: generate → stream → save → edit → rewrite
- Phase navigation and progress bar are visible and accurate
</success_criteria>

<output>
After completion, create `.planning/phases/03-chapter-generation/03-05-SUMMARY.md`
</output>
