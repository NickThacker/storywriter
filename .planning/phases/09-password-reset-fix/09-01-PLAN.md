---
phase: 09-password-reset-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(reset)/layout.tsx
  - src/app/(reset)/auth/reset-password/page.tsx
  - src/app/(auth)/auth/reset-password/page.tsx
  - src/actions/auth.ts
  - src/components/auth/reset-password-form.tsx
  - src/app/(auth)/login/page.tsx
  - src/components/auth/auth-form.tsx
autonomous: true
requirements:
  - PRST-01
  - PRST-02

must_haves:
  truths:
    - "Recovery link from Supabase email lands on /auth/reset-password with a valid session and shows the set-new-password form"
    - "Visiting /auth/reset-password without an authenticated session redirects to /login?error=session_expired with error message"
    - "User can enter and confirm a new password, sees success confirmation with countdown, then is redirected to dashboard"
    - "Login page shows 'session expired' error with forgot-password form auto-opened when redirected from expired reset link"
  artifacts:
    - path: "src/app/(reset)/auth/reset-password/page.tsx"
      provides: "Server component with session guard"
      contains: "supabase.auth.getUser"
    - path: "src/app/(reset)/layout.tsx"
      provides: "Minimal centering layout without auth redirect"
      contains: "min-h-screen"
    - path: "src/components/auth/reset-password-form.tsx"
      provides: "Form with success state and countdown redirect"
      contains: "Password updated"
    - path: "src/actions/auth.ts"
      provides: "updatePassword returning success state instead of redirect"
      contains: "success: true"
  key_links:
    - from: "src/app/(reset)/auth/reset-password/page.tsx"
      to: "src/components/auth/reset-password-form.tsx"
      via: "renders ResetPasswordForm after session guard passes"
      pattern: "ResetPasswordForm"
    - from: "src/components/auth/reset-password-form.tsx"
      to: "src/actions/auth.ts"
      via: "useActionState calling updatePassword"
      pattern: "useActionState.*updatePassword"
    - from: "src/app/(auth)/login/page.tsx"
      to: "src/components/auth/auth-form.tsx"
      via: "initialMode prop for session_expired error"
      pattern: "initialMode"
---

<objective>
Fix the password reset flow so recovery links work end-to-end: user clicks link, sees the reset form, sets a new password, gets a success confirmation with countdown, and is redirected to the dashboard.

Purpose: The current flow is broken because `(auth)/layout.tsx` redirects authenticated users (including recovery users) to `/dashboard` before they see the reset form, and `updatePassword` calls `redirect()` which prevents showing a success state.

Output: Working password reset flow with server-side session guard, success countdown, and proper error handling for expired/invalid links.
</objective>

<execution_context>
@/Users/nickthacker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickthacker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-password-reset-fix/09-CONTEXT.md
@.planning/phases/09-password-reset-fix/09-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/actions/auth.ts (current signature — will be modified):
```typescript
export async function updatePassword(
  prevState: { error?: string } | null,
  formData: FormData
): Promise<{ error: string } | never>
// Currently calls redirect('/dashboard') on success — needs to return { success: true } instead
```

From src/components/auth/reset-password-form.tsx (current type — will be expanded):
```typescript
type ActionState = { error?: string } | null
// Needs to become: { error?: string; success?: boolean } | null
```

From src/components/auth/auth-form.tsx:
```typescript
type AuthMode = 'sign-in' | 'sign-up' | 'forgot-password'
// AuthForm currently accepts: { initialError?: string }
// Needs new prop: initialMode?: AuthMode
```

From src/app/(auth)/layout.tsx (NOT modified — this is the problem):
```typescript
// Redirects authenticated users to /dashboard — this is WHY we need a new route group
const { data: { user } } = await supabase.auth.getUser()
if (user) { redirect('/dashboard') }
```

From src/app/(auth)/login/page.tsx (current error handling):
```typescript
const error =
  params.error === 'invalid_token'
    ? 'The verification link is invalid. Please try again.'
    : params.error === 'expired_link'
    ? 'This link has already been used or has expired. Please request a new one.'
    : undefined
// Needs session_expired case added
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move reset-password to (reset) route group with session guard</name>
  <files>
    src/app/(reset)/layout.tsx,
    src/app/(reset)/auth/reset-password/page.tsx,
    src/app/(auth)/auth/reset-password/page.tsx
  </files>
  <action>
1. Create `src/app/(reset)/layout.tsx` — a minimal centering layout that does NOT redirect authenticated users. Copy the centering wrapper from `(auth)/layout.tsx` (`min-h-screen flex items-center justify-center bg-background` + inner `w-full max-w-md px-4` div) but without any session check or redirect. This is a server component (no 'use client').

2. Create `src/app/(reset)/auth/reset-password/page.tsx` — a server component that:
   - Imports `createClient` from `@/lib/supabase/server` and `redirect` from `next/navigation`
   - Calls `supabase.auth.getUser()` to check for an authenticated session
   - If no user: `redirect('/login?error=session_expired')` — no flash of form
   - If user exists: renders `<ResetPasswordForm />` from `@/components/auth/reset-password-form`
   - Exports metadata: `{ title: 'Reset Password — Meridian' }`

3. Delete the old page at `src/app/(auth)/auth/reset-password/page.tsx` (it will be replaced by the new one in `(reset)`).

IMPORTANT: Do NOT modify `(auth)/layout.tsx` — it correctly redirects authenticated users away from login/signup. The fix is moving reset-password OUT of that route group. Do NOT remove `/auth/reset-password` from `PUBLIC_PATHS` in middleware — it must stay there.
  </action>
  <verify>
    <automated>ls -la src/app/\(reset\)/layout.tsx src/app/\(reset\)/auth/reset-password/page.tsx && ! ls src/app/\(auth\)/auth/reset-password/page.tsx 2>/dev/null && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - `(reset)/layout.tsx` exists with centering wrapper and no auth redirect
    - `(reset)/auth/reset-password/page.tsx` exists with server-side session guard using `getUser()`
    - Old `(auth)/auth/reset-password/page.tsx` is deleted
    - The URL `/auth/reset-password` still resolves (route group names don't affect URLs)
  </done>
</task>

<task type="auto">
  <name>Task 2: Success state with countdown, action fix, and login error handling</name>
  <files>
    src/actions/auth.ts,
    src/components/auth/reset-password-form.tsx,
    src/app/(auth)/login/page.tsx,
    src/components/auth/auth-form.tsx
  </files>
  <action>
1. **Update `src/actions/auth.ts` — `updatePassword` function:**
   - Change return type from `Promise<{ error: string } | never>` to `Promise<{ error: string } | { success: boolean }>`
   - Remove the `redirect('/dashboard')` call at the end
   - Replace with `return { success: true }`
   - Keep all existing validation and session check logic unchanged

2. **Update `src/components/auth/reset-password-form.tsx`:**
   - Expand `ActionState` type to: `{ error?: string; success?: boolean } | null`
   - Add imports: `useEffect`, `useState` from 'react'; `useRouter` from 'next/navigation'; `CheckCircle` from 'lucide-react'
   - Create a `SuccessState` inner component (per CONTEXT.md):
     - Uses `useRouter()` and `useState(3)` for countdown
     - `useEffect` with `setInterval` that decrements every second
     - When counter hits 0, `clearInterval` and `router.push('/dashboard')`
     - Cleanup: `return () => clearInterval(interval)` in useEffect
     - Renders: `CheckCircle` icon (h-10 w-10 text-green-500), "Password updated!" bold text, "Redirecting to dashboard in {seconds}..." muted text
     - Add a "Continue now" text link below countdown that calls `router.push('/dashboard')` immediately
   - In the main `ResetPasswordForm` component:
     - Check `state && 'success' in state && state.success` — if true, render `<SuccessState />` instead of the form
     - Wrap in the same Card/branding container so it looks consistent

3. **Update `src/app/(auth)/login/page.tsx`:**
   - Add `session_expired` case to the error conditional:
     ```
     params.error === 'session_expired'
       ? 'Your reset link has expired. Request a new one below.'
     ```
   - Pass `initialMode` prop to `AuthForm`:
     ```
     <AuthForm initialError={error} initialMode={params.error === 'session_expired' ? 'forgot-password' : undefined} />
     ```

4. **Update `src/components/auth/auth-form.tsx`:**
   - Add `initialMode?: AuthMode` to the component props interface
   - Update the `useState<AuthMode>('sign-in')` call to use `initialMode ?? 'sign-in'` as default
   - This way, when redirected with `session_expired`, the form auto-opens in forgot-password mode — this IS the "action button" behavior specified in CONTEXT.md
  </action>
  <verify>
    <automated>cd /Users/nickthacker/storywriter && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - `updatePassword` returns `{ success: true }` instead of calling `redirect('/dashboard')`
    - `ResetPasswordForm` shows success state with CheckCircle icon, "Password updated!" message, and countdown from 3
    - Success state auto-redirects to `/dashboard` after 3 seconds via `router.push`
    - Login page handles `session_expired` error param with message "Your reset link has expired. Request a new one below."
    - Login page auto-opens in `forgot-password` mode when `session_expired` error is present
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `npx tsc --noEmit` passes
2. Route structure: `(reset)/auth/reset-password/page.tsx` exists with session guard; old `(auth)/auth/reset-password/page.tsx` deleted
3. No auth redirect trap: `(reset)/layout.tsx` does NOT contain `getUser` or `redirect('/dashboard')`
4. Success flow: `updatePassword` in `auth.ts` returns `{ success: true }`, NOT `redirect('/dashboard')`
5. Error handling: `login/page.tsx` handles `session_expired` error param
6. Form: `reset-password-form.tsx` has success state with countdown and CheckCircle icon
</verification>

<success_criteria>
- The password reset flow works end-to-end: recovery link -> authenticated session -> /auth/reset-password shows form -> submit -> success countdown -> dashboard
- Unauthenticated visitors to /auth/reset-password are redirected to /login with session_expired error
- Login page shows the expired link error and auto-opens forgot-password mode for easy re-request
- No TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/09-password-reset-fix/09-01-SUMMARY.md`
</output>
