'use server'

// Placeholder signOut — will be fully implemented in Plan 02 (Auth)
// The dashboard layout imports this for the Sign out button.
export async function signOut() {
  // Plan 02 will implement: supabase.auth.signOut() + redirect('/login')
  // Left as placeholder so dashboard layout can compile before Plan 02 executes.
}
